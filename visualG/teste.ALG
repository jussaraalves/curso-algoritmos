Algoritmo "teste"

Var
   x,y,z,a,b,c,d,f,delta : real

Inicio

      x <- 2.0
      y <- 3.0
      z <- -7.0
      
      a <- Exp(x,y)
      b <- RaizQ(y)
      c <- pi
      d <- Abs(z)
      
      escreval(a)
      escreval(b)
      escreval(c)
      escreval(d)

Fimalgoritmo